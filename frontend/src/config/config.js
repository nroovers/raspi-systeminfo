const config = {
    nodes: [
        {
            name: 'Raspberry Pi',
            apiUrlPref: '/api'
        },
        {
            name: 'Synology Nas',
            apiUrlPref: '/api-nas'
        }
    ]
}

export default config