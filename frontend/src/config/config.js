const config = {
    nodes: [
        {
            name: 'Raspberry Pi',
            apiUrlPref: '/api-raspi'
        },
        {
            name: 'Synology Nas',
            apiUrlPref: '/api-nas'
        }
    ]
}

export default config